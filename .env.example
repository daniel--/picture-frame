# Required
JWT_SECRET=
DB_FILE_NAME=file:local.sqlite

# Optional
PORT=3000
NODE_ENV=development
APP_NAME=Family Photos
APP_URL=https://your-domain.com

# Email (required if EMAIL_ENABLED=true)
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_FROM_NAME=
